# Environment Variables for Agent Passport API
# Copy this file to .env and fill in the values

# Environment
NODE_ENV=development
PORT=3001

# Database (Neon Postgres)
DATABASE_URL="postgresql://user:password@host.neon.tech/database?sslmode=require"

# Redis (Upstash)
REDIS_URL="rediss://default:password@host.upstash.io:6379"

# JWT Signing Key (generate using: pnpm -C apps/passport-api tsx scripts/gen-jwk.ts)
JWT_SIGNING_JWK_JSON='{"kty":"OKP","crv":"Ed25519","x":"...","d":"..."}'

# Token TTL in minutes (default: 60)
TOKEN_TTL_MINUTES=60

# Challenge TTL in minutes (default: 5)
CHALLENGE_TTL_MINUTES=5

# CORS allowed origins (comma-separated, or * for all)
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-portal.vercel.app

# Portal internal API key (for portal -> API communication)
# Generate a secure random string (at least 32 characters)
PORTAL_INTERNAL_API_KEY=your-secure-random-string-at-least-32-chars
