# Environment Variables for Agent Passport Portal
# Copy this file to .env.local and fill in the values

# Public API URL (client-side accessible)
NEXT_PUBLIC_PASSPORT_API_BASE_URL=https://your-api.onrender.com

# Session encryption secret (generate a secure random string, 32+ chars)
PORTAL_AUTH_SECRET=your-session-secret-at-least-32-characters

# Optional: Internal API key for server-to-server calls to passport-api
PORTAL_INTERNAL_API_KEY=your-internal-api-key
